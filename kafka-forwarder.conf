{
  "network": {
    "servers": [ "kafkaservername:9092" ],
	"topic":"ls-kafka-topic"
  },
  "files": [
    {
      "paths": [ "somedir/*.log" ],
      "fields": { "type": "disklogfiles" }
    }
  ]
}
